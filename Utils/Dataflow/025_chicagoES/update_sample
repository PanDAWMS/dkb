#!/usr/bin/env bash

base_dir=$(readlink -f $(dirname "$0"))

lib=$base_dir/../shell_lib
. $lib/update_sample

sample=$base_dir/output/sample.ndjson

$base_dir/stage.py -d s $base_dir/input/sample.ndjson > $sample.new

update_sample $sample{,.new}
