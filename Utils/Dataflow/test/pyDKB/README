=============
* Stage XX  *
=============

1. Description
--------------
pyDKB testing stage.

Run as:

  ./json2TTL.py -i input
  ./json2TTL.py input/196.json
  cat << EOF | ./json2TTL.py -m s | less
  {"a": 1}
  {"b": 2}
  EOF
  ./json2TTL.py --hdfs -i /my/test/input

etc

2. What to check
----------------

Test cases:

(-) without parameters (expected: "no input file specified" error):

  ./json2TTL.py
